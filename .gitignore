# --- Python ---
__pycache__/
*.py[cod]
*.egg-info/
.venv/

# --- Secrets (NEVER commit these) ---
# config.yaml holds your DataForSEO login/password + live site values.
config.yaml
credentials/
# The real .env (Docker deploy secrets); .env.example IS safe to commit.
.env
.env.local

# --- Local data / generated artifacts ---
data/*.db
data/*.log
reports/
drafts/

# --- macOS ---
.DS_Store
